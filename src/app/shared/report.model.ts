import { AdsPackage } from "./adsPackage.model"
import { Invoice } from "./invoice.model"

export class ReportModel {
    memberCount: number
    memberToday: number
    invoiceCount: number
    invoiceToday: number
    adPackageCount: number
    adPackageToday: number
    categoryCount: number
    
}

